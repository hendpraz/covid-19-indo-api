export default async function(region) {


    // cek di database apakah itemnya ada dan tidak expired,
    // kalau ada dan belum expire, ambil di database aja

    // Nilai Return
    // [] = Empty array apabila sudah expired atau tidak ditemukan item
}